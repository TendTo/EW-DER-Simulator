{
  "name": "ew-simulation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "start": "npm start --prefix simulator",
    "compile": "npm run compile --prefix contracts",
    "test": "npm test --prefix contracts",
    "watch": "npm run watch --prefix simulator",
    "coverage": "npm run coverage --prefix contracts",
    "deploy:volta": "npm run deploy:volta --prefix contracts"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "contracts",
    "simulator"
  ]
}